export const customDayString = (day) => {
    const string = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "other"];

    const custom = string[day]
    
    return(
        custom
    )
}